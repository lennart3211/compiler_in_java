package compiler.Token;

public enum TokenType {
    FUN,
    INT,
    OPEN_PAREN,
    CLOSE_PARAN,
    OPEN_CURLY,
    CLOSE_CURLY,
    PLUS,
    MINUS,
    ASTERISK,
    F_SLASH,
    EQUALS,
    NOT,
    SEMI,
    RETURN,
    NAME,
    EXIT,
    LET
}
